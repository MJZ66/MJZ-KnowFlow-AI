# 03 — 后端验收

## 1. API 端到端验收

```powershell
cd E:\AI知识平台\knowflow-ai\backend
python scripts\e2e_acceptance.py
```

预期：**13/13 passed**

覆盖：注册、KB 创建、中文文档 COMPLETED、空文档 FAILED、RAG SSE、references 噪声过滤、i18n 键 parity 等。

## 2. pytest

```powershell
cd E:\AI知识平台\knowflow-ai
docker compose exec backend pytest tests/ -v
```

当前基线：**37 passed, 4 skipped**

## 3. 检索质量验收

```powershell
docker compose exec backend python scripts/retrieval_quality_check.py
```

临时开启 Hybrid：

```powershell
docker compose exec backend sh -c "HYBRID_SEARCH_ENABLED=true python scripts/retrieval_quality_check.py"
```

临时开启 Hybrid + Reranker：

```powershell
docker compose exec backend sh -c "HYBRID_SEARCH_ENABLED=true RERANKER_ENABLED=true python scripts/retrieval_quality_check.py"
```

## 相关章节

- [05 — 数据库检查](./05-数据库检查.md)
- [06 — 环境变量](./06-环境变量.md)
- [13 — 团队流程](./13-团队流程.md)
