# Development — copy to .env for local Docker Compose / dev server
# cp .env.development.example .env
# Same as .env.example; kept as a named template for dev/staging/prod split.

# ============================================
# Database
# ============================================
POSTGRES_USER=knowflow
POSTGRES_PASSWORD=knowflow_secret
POSTGRES_DB=knowflow
POSTGRES_PORT=5432

# ============================================
# Redis
# ============================================
REDIS_PORT=6379
REDIS_URL=redis://redis:6379/0

TASK_BACKEND=celery
RAG_CACHE_ENABLED=true
RAG_CACHE_TTL_SECONDS=300
METRICS_ENABLED=true

ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000
MAX_UPLOAD_SIZE_MB=30
ALLOWED_FILE_EXTENSIONS=pdf,docx,md,txt

CHROMA_PORT=8001

SECRET_KEY=change-me-to-a-random-secret-key-at-least-32-chars
APP_ENV=development
RUN_MIGRATIONS_ON_START=true
BACKEND_PORT=8000

LLM_PROVIDER=qwen
LLM_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=your_qwen_api_key_here
LLM_MODEL=qwen-plus

VECTOR_PROVIDER=chroma_hash
EMBEDDING_PROVIDER=hash
EMBEDDING_DIM=384

HYBRID_SEARCH_ENABLED=false
RERANKER_ENABLED=false

FRONTEND_PORT=3000
VITE_API_BASE_URL=http://localhost:8000
